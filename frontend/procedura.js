// Script para dibujar bloques aleatorios en el mapa
// Los bloques se generan en el servidor y se envían a los clientes

// Variable global para almacenar los bloques (se recibe del servidor)
window.bloquesAleatorios = [];

// Función para dibujar los bloques
function dibujarBloquesAleatorios(ctx, offsetX, offsetY) {
  // Bloques procedurales desactivados: no dibujar nada
  return;
}